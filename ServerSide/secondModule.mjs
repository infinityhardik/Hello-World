import { h } from './firstModule.mjs';

console.log(h, 'Hardik');
// use .MJS extension for using ES6 Syntax of Import - Export

/* Else use > "type": "module", > in the package.json file to use .js Extension*/ 
