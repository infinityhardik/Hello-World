function add(a, b) {
  return a + b;
}

function subtract(a, b) {
  return a - b;
}

function multiply(a, b) {
  return a * b;
}

function divide(a, b) {
  return a / b;
}

// Don't change above code
/*1. Export all these functions from that file.*/

module.exports = {
  add,
  subtract,
  multiply,
  divide,
};

// Basics of Import - Export